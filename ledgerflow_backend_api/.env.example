# Database and Auth
DATABASE_URL="mysql://USER:PASSWORD@HOST:PORT/DBNAME"
JWT_SECRET="replace-with-strong-random-secret"

# GitHub App Integration (placeholders - do not commit real secrets)
# If using GitHub App for installation-based access:
GITHUB_APP_ID="your-github-app-id"
# For PKCS#1 or PKCS#8 private key, provide it with proper escaping or load from secrets manager.
# Example: set in environment rather than .env if multiline causes issues.
GITHUB_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nREDACTED\n-----END PRIVATE KEY-----"
# Webhook secret used to verify signatures (x-hub-signature-256)
GITHUB_WEBHOOK_SECRET="replace-with-random-webhook-secret"
